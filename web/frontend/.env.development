# En mode dev (vite), le frontend tourne sur :5173 et appelle le backend sur :8000.
# En build (mono-process), VITE_API_URL n'est pas défini → URLs relatives même origine.
VITE_API_URL=http://localhost:8000
